<h1>Activities</h1>
<% @activities.each do |activity| %>
	<%= link_to activity.owner.username, activity.owner if activity.owner %> applied to 
	<%= link_to activity.trackable.project_name , activity.trackable %> <br/>
<% end %>
